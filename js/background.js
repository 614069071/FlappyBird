/**
 * /
 * @param {[type]} img  [背景图片地址]
 * @param {[type]} step [步长]
 * @param {[type]} x    [背景x位置]
 * @param {[type]} y    [背景y位置]
 */
function Background(img,step,x,y) {
	this.img = img;
	this.step = step;
	this.x = x;
	this.y = y;
}